# verity.ai: remaining tasks

Flat list, no milestones. Split by frontend / backend only. File paths are real, pulled from the actual repo.

## Topic scope

**Math (grades 6-12), chosen because they're deterministically checkable:**
1. Linear equations & inequalities, one variable
2. Systems of linear equations, two variables (substitution/elimination)
3. Quadratics and polynomial factoring/simplification
4. Exponent rules, integer exponents
5. Basic differentiation (power rule, sum rule)
6. Basic integration (reverse power rule, up to +C)

Not in scope: geometry, proofs, anything without a single symbolic right answer to check against.

**Chemistry, checkable via RDKit + a vision model:**
1. Molecular structure matching (already built)
2. Functional group identification
3. Balancing chemical equations
4. Redox half-reaction electron balancing
5. Empirical/molecular formula from percent composition
6. Hand-drawn structure recognition (drawing → SMILES, currently missing entirely)

---

## Backend

- [ ] `backend/judge/algebra.py`: add inequality parsing and checking (`>`, `<`, `>=`, `<=`) alongside the existing `=` handling
- [ ] `backend/judge/algebra.py`: remove the `^`/`**` block for simple integer exponents, add support for checking exponent-rule steps (`x^2 * x^3 = x^5` type equivalence)
- [ ] `backend/judge/algebra.py`: extend `_support_reason` to allow degree-2 polynomials instead of rejecting anything with `Pow`
- [ ] new file `backend/judge/systems.py`: two-variable linear system judge — checks substitution/elimination steps against a system of two equations
- [ ] new file `backend/judge/quadratics.py` (or extend `algebra.py`): factoring and quadratic-equivalence checking
- [ ] new file `backend/judge/calculus.py`: differentiation judge — compares a submitted derivative against `sympy.diff()` of the reference
- [ ] `backend/judge/calculus.py`: integration judge — compares a submitted antiderivative against `sympy.integrate()`, accounting for the arbitrary `+C`
- [ ] `backend/judge/chemistry.py`: add a functional-group comparison mode (substructure match, not full-molecule equivalence) — currently only does whole-molecule SMILES equality
- [ ] new file `backend/judge/chemistry_equations.py`: chemical equation balancer — parse both sides of a reaction string, check atom counts and charge balance per side (pure parsing/arithmetic, doesn't need RDKit)
- [ ] new file `backend/judge/redox.py` (or extend `chemistry_equations.py`): half-reaction electron-balance checker
- [ ] new file `backend/structure_recognition.py`: Gemini vision call that converts a hand-drawn/photographed chemistry structure image into a SMILES string, same pattern as `backend/transcription.py` but chemistry-specific
- [ ] `backend/schemas.py`: add request/response models for systems, quadratics, calculus, chemical-equation balancing, redox, and structure recognition
- [ ] `backend/main.py`: register new endpoints — `/check/systems`, `/check/calculus`, `/chemistry/balance`, `/chemistry/redox`, `/chemistry/transcribe`
- [ ] `backend/hints.py`: add error categories and templates for `combining_like_terms`, `dropped_term`, `swapped_sides`, `one_side_only` (algebra), plus category coverage for systems/quadratics/calculus errors
- [ ] `backend/hints.py`: add chemistry-specific hint templates — currently `structure_mismatch` (and any new chemistry error types) fall through to the generic algebra fallback text, which doesn't make sense for chemistry
- [ ] `backend/judge/base.py`: confirm the generic `Judge` interface still fits once systems/calculus/chemistry-equations judges exist, adjust if not
- [ ] new test files: `backend/tests/test_systems_judge.py`, `test_quadratics_judge.py`, `test_calculus_judge.py`, `test_chemistry_equations.py`, `test_redox_judge.py`, `test_structure_recognition.py`
- [ ] `backend/tests/transcription/failures.md`: keep logging real failures as you test the new structure-recognition endpoint, same as the math transcription log

## Frontend

- [ ] `frontend/src/App.jsx`: split into components — it's currently one 1906-line file with all state, canvas logic, and UI in a single component. At minimum pull out the canvas/segmentation logic, the verdict/hint panel, and the problem-input UI into separate files
- [ ] `frontend/src/App.jsx` (`segmentIntoLines`, `getStrokeRow`): still pure row-bucketing — implement real pen-lift + vertical-gap segmentation
- [ ] `frontend/src/App.jsx`: add a topic/subject selector (linear, systems, quadratics, exponents, calculus, chemistry) that changes which backend endpoint a finished line gets sent to
- [ ] `frontend/src/App.jsx`: build the entire chemistry mode — there is currently zero chemistry code in the frontend. Needs: a way to draw/label molecular structures on canvas, wiring finished chemistry lines to `/chemistry/transcribe` then `/chemistry/check`, and a chemistry-specific verdict display
- [ ] `frontend/src/App.jsx`: update hint display logic to handle the new error categories once `backend/hints.py` adds them
- [ ] `frontend/src/App.jsx`: add UI for the chemical-equation-balancing and redox modes once those endpoints exist (likely a different input method than freehand structure drawing — typed formula input may be more reliable than drawn)

## Cleanup / repo hygiene (do this before adding more files, not after)

- [ ] `CLAUDE.md` and `backend/tests/transcription/results.txt` are currently untracked locally — decide if they're meant to be committed or gitignored, don't leave them floating
- [ ] `backend/start_backend.ps1` is an intentional backward-compat wrapper around `start-backend.ps1` — not dead code, leave it
- [ ] Confirm `backend/tests/transcription/samples/*.png` are all still referenced by `run_samples.py` before adding new chemistry sample images alongside them, so the two don't get mixed up
