{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://verityai.example/schemas/handwriting-fixture-v1.json",
  "title": "VerityAI handwriting evaluation fixture",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "domain",
    "topic",
    "difficulty",
    "inputs",
    "expected",
    "tags",
    "annotation",
    "consent"
  ],
  "properties": {
    "schema_version": {
      "const": 1
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._-]+$"
    },
    "domain": {
      "enum": ["math", "chemistry_text", "chemistry_structure"]
    },
    "topic": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "pattern": "^[a-z0-9][a-z0-9._-]+$"
    },
    "difficulty": {
      "enum": ["basic", "intermediate", "advanced", "ambiguous"]
    },
    "device_group": {
      "type": "string",
      "maxLength": 80,
      "pattern": "^[a-z0-9][a-z0-9._-]+$"
    },
    "browser_group": {
      "type": "string",
      "maxLength": 80,
      "pattern": "^[a-z0-9][a-z0-9._-]+$"
    },
    "inputs": {
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "strokes": {
          "type": "string",
          "pattern": "\\.json$"
        },
        "image": {
          "type": "string",
          "pattern": "\\.png$"
        }
      }
    },
    "expected": {
      "type": "object",
      "additionalProperties": false,
      "required": ["format", "canonical", "unreadable"],
      "properties": {
        "format": {
          "enum": ["ascii", "latex", "jiix", "smiles", "text"]
        },
        "canonical": {
          "type": "string"
        },
        "accepted": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true
        },
        "unreadable": {
          "type": "boolean"
        }
      }
    },
    "tags": {
      "type": "array",
      "items": {
        "enum": [
          "fraction",
          "nested_fraction",
          "superscript",
          "subscript",
          "matrix",
          "integral",
          "limit",
          "greek",
          "chemistry_capitalization",
          "charge",
          "reaction_arrow",
          "one-l-i",
          "zero-o",
          "x-times",
          "minus-equals",
          "decimal_point",
          "cursive",
          "cramped",
          "overwritten",
          "edited_out_of_order"
        ]
      },
      "uniqueItems": true
    },
    "annotation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reviewer_count", "status"],
      "properties": {
        "reviewer_count": {
          "type": "integer",
          "minimum": 1
        },
        "status": {
          "enum": ["draft", "reviewed", "adjudicated"]
        },
        "notes": {
          "type": "string",
          "maxLength": 240
        }
      }
    },
    "consent": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "retention_approved",
        "retention_policy_id",
        "source",
        "provenance_id",
        "approved_providers"
      ],
      "properties": {
        "retention_approved": {
          "type": "boolean"
        },
        "retention_policy_id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9._-]+$",
          "maxLength": 80
        },
        "source": {
          "enum": ["synthetic", "internal", "consented_user"]
        },
        "provenance_id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9._-]+$",
          "maxLength": 80
        },
        "approved_providers": {
          "type": "array",
          "minItems": 1,
          "maxItems": 20,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._-]+$"
          },
          "uniqueItems": true
        }
      }
    }
  }
}
